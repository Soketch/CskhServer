/**
 * 关注/喜欢服务
 */

import { UserWithRelations, UserFollow, UserLike } from '@/interface';

class FollowerService {

    // 返回id数组
    async getFollowers(userId: number){
       
    }

    // 返回 id 数组
    async getByFollowers(followerId: number){
    }

    // 关注

    // 取消关注

    // 喜欢

    // 取消喜欢
}


export default new FollowerService();